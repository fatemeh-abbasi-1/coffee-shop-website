# Input

Custom input component with `primary` and default variants.

## Props

- type: "text" or "password"
- value
- onChange
- onFocus
- onBlur
- placeholder
- className
- variant
- ref: forwarded

## Usage

- Default: `<Input variant={} type={}/>`
